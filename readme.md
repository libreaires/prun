# prun: a random cleaning tool, that doesn't even currently work outside of a NixOS system...

Just a silly stupid and small (3 characteristics!) .sh program for cleaning MY (my specificly) desktop (NixOS) setup (I'm using to many parenthesis).

# Using

Simply run start.sh... here is a step-by-step of cloning and using this repo:

```shell
git clone https://github.com/libreaires/prun "prun"
cd prun
bash start.sh
```